%h1 Editing prompt_algorithm

-form_for(@prompt_algorithm) do |f|
  = render :partial => 'form', :locals => { :f => f }
  %p
    = f.submit "Update"
= link_to 'Show', @prompt_algorithm
|
= link_to 'Back', prompt_algorithms_path
